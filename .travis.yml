language: go

go:
  - "1.11.x"

env:
  - GO111MODULE=on

install:
  - go version
  - make init
  - go get github.com/mattn/goveralls

script:
  - make test
  - $GOPATH/bin/goveralls -service=travis-ci -coverprofile=coverage.out

